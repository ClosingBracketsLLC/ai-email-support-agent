# Phase 3 — whole-branch final review record

Branch `phase-3`, base `a613a22` (`main`) → reviewed at `3c76a8a` (39 commits, 216 files, +33,439/−453), fixed through `18c66d6`. Six parallel reviewers on per-area `-U10` packages (worker drafting; worker sending + the E2E; packages; api; app; cross-cutting seams + docs), the seams read in place. Gate at the reviewed commit: 1,572 tests, 21 web routes, the 21-case Phase 3 E2E and the Playwright smoke green; after the wave: 1,641 tests plus 4 conditional skips (contracts 21, crypto 42, platform-mail 18, core 204, llm 98, agent 65, db 72, queue 17, mail 237, test-kit 39 + 4 skipped, api 210, worker 357, app 261), 21 routes, `db:check` clean.

## Verdict

Approved for the PR after the fix wave. Critical: none. Important: 13 from the six reviews plus 3 surfaced by the wave's own re-reviews, all fixed. The wave is eight commits (`176f717` packages; `b776159`, `0cf7c1c`, `064e198` and `18c66d6` api, the last two from the wave's own re-reviews; `2aebfde` worker; `0e444c6` app; `7249c44` docs) on top of the pre-review fix `883ecac`. One execution-time ruling was reversed (the approve gate's policy). Nineteen carried minors from the task reviews and the reviews' own carries are listed in STATUS.md's Phase 3 record; Robert's outstanding manual steps are in the Phase 3 runbook.

## What the branch-level review found (all fixed in the wave unless marked carry)

Important (13 + 3):
- Packages: 5-minute cache writes were billed at the 1-hour rate (one `cacheWriteTokens` priced at 2×) — split by TTL from the SDK's `cache_creation` breakdown. The guardrail strip removed only `\p{Cf}`, so `Mn` default-ignorables (U+FE0F, U+034F) defeated five FAIL screens — now `\p{Default_Ignorable_Code_Point}` before and after NFKC (emoji lose their VS-16 presentation; documented). `JSON.stringify` leaves U+2028/9 unescaped, so a hostile body could forge a thread line — escaped.
- Api: the approve gate screened with no trusted texts (an execution-time ruling) while the send gate screened with four, so an owner edit quoting the guidance passed Approve and was destroyed at send — the ruling is reversed; one pure policy builder at `@aesa/agent/policy` serves all three gates (a module-graph test proves the api never loads the Anthropic SDK). `resolveTicket` left a `held` draft live, so the next draft insert hit the partial unique forever — every live draft is retired and `queued|claimed` sends are held. A dead-lettered draft was terminal with no return path while the runbook promised a re-approve — `failed → pending` through resume, the ticket back to review, refused while a live draft exists and only while the ticket is still the owner's (`needs_owner/send_failed` or `triaged`); the ticket read falls back to the newest failed draft under the same condition.
- Worker: a ticket at the failure ceiling that a send hand-back returned to `triaged` was stranded with no signal — the hand-backs reset the count and the sweep pages it. The release delay (60 s) outran pg-boss's first retry, so the dead-letter was unreachable on the release paths — 15 s with a startup invariant. The backstop sweep re-enqueued under the global killswitch — it skips under the lever. A `claimed` send's pre-send flip never re-checked the draft — it now requires the draft live and lands `failed` (never re-picked) when it is not. `singletonKey` was inert under pg-boss 10's default `standard` policy (its unique indexes are policy-gated) — the four Phase 3 queues use `policy: 'short'`, proven both ways with a real broker.
- App: the ticket screen stopped polling after a redraft (a rejected draft is not live), so the promised new draft never appeared — it polls while the ticket is `triaged` with no live draft. A failed viewed-mark disabled Approve for the visit — one automatic retry plus a manual control.

Minor (promoted to the wave): the digest minted a token per pending draft (now only the ten rendered); a raw `{ err }` log in the sandbox job could carry the owner's text through pg's "Failing row" message; `packages/db`'s runtime dependency on `@aesa/llm` for a type; `@fastify/formbody` on the shared context; the claim-time email on an idempotent re-claim; three send landings paging on a 0-row guarded update; four missing llm assertions; the disabled-agent and go-live copy; `formatUsd` on a negative; comment fixes.

## Rulings

32 execution-time rulings were reviewed: one contradicted the spec (the approve-gate policy) and was reversed; none contradicted another. The wave added six rulings (all in STATUS.md): the shared policy; the failed-draft return path; every live draft retired on resolve and the flip's draft re-check; the killswitch-aware sweep with the org-cap loop carried; the `short` queue policy; a resume during an in-flight redraft allowed (it self-corrects at the send gate).

## What the review verified holds

The claim protocol (row-locked CAS, three watermarks, the stuck branch alone charging a failure, the ceiling escalation inside the claim tx) and the advisory-locked caps, both mutation-tested; the draft job's fifteen rules with the model call outside every transaction and `stampFinished` on every authoritative outcome; the send job's twelve steps and three crash windows with the recovery scan first, the completion gated on the guarded update, and the global lock order `outbound_sends → drafts → tickets` across the api and the worker (the four inversions found were closed and a one-shot deadlock retry sits outside `withOrg`); RLS enabled and forced on every new table with the exact policies, the CHECKs matching the contracts enums, the partial unique on live drafts, and the SECURITY DEFINER token resolver's grants; `decide()` clause by clause against the spec with the auto branch unreachable; the review pages provably read-only on GET, oracle-free, escaped in every context, `no-store` with a referrer policy, rate-limited, tokens never logged; the app's import rule and test hygiene (no fake timers, self-contained mocks, no new act warnings, 21 routes); the E2E's realness (a real broker, the api's real approve gate, the fake provider under the real metering sink — which is how the `llm_calls` float-into-integer defect surfaced); the docs' truthfulness after Task 23's fixes.

## Residuals carried to Phase 4

Listed one per line in STATUS.md's Phase 3 record: the org-cap sweep loop (bounded, ends at UTC midnight); `structuredOutput: 'none'` providers (Phase 6); the U+FE0F/tag-character flatten and `blocks.ts` still telling the model bidi controls are rejected rather than stripped; the undo-window re-approve landing ≤2 minutes late through the sweep; keyless sends collapsing on `short` queues (test-only today); a resume during an in-flight redraft (wasteful, self-correcting); `sweeps.daily` holding cross-org draft locks through its escalation loop; the push Hold action inert until Phase 5's auto-send; the inbox keyset cursor (Phase 2 inherited); no CSP/XFO on the review pages; the hand-authored cache fixture until Robert records it; the ticket screen's single note slot hiding the viewed-mark retry control; the a11y minors.

## Process note

A 33k-line branch does not fit one reviewer: the six-way split (five area packages plus one seams reviewer reading code in place) found what the 23 task reviews could not — cross-gate policy drift, a stranded-ticket path across two jobs, a queue-policy assumption inherited from Phase 2. The wave ran one implementer per area in sequence (a shared git index forbids parallel writers), with re-reviews overlapping the next implementer; two of the wave's own fixes needed a second and third round on the api. Watch item: E2E scenario 10 flaked once on a loaded box (the follow-up re-draft's wait) and passed on every re-run.
