# Brand — whole-branch final review record

Branch `brand`, base `7a40be2` (`phase-3`) → reviewed at `d59d920` (10 commits, 104 files, +4,720/−369: the spec `0f3efbe`, the derivation `963d21e`, the plan `d604089`, the seven tasks `b736516`, `e564be4`, `b2fa0c9`, `073e9f5`, `658b70e`, `1d843c7`, `d59d920`), fixed through `9b50dcd`. One reviewer on the most capable model over a single `-U10` package of the whole range, with the per-task reviews' 20 deferred minors handed over for triage. Gate at the reviewed commit: 1,787 tests plus 4 conditional skips, 21 web routes, the Playwright signup smoke green, `db:check` clean; after the wave: 1,788 tests plus 4 skips, 21 routes, the web export carrying exactly the five brand faces (5 `.ttf`, down from 32), the smoke green.

## Verdict

Approved for the PR after the fix wave. Critical: none. Important: 3, all fixed. The wave is three commits — `4a7f110` (app: per-weight font subpaths, the AA-safe active sidebar label, the tab-bar weight cue, `Chip` adopted for the category pills), `78e8ec4` (api: the comment at the review page's constant interpolations), `9b50dcd` (brand/docs: Lucide's ISC licence file, the tightened colour guard, doc corrections, the fix-wave paragraph in STATUS) — and its scoped re-review verdicted all 13 items addressed with no new Critical or Important breakage. Eighteen of the twenty deferred minors stay deferred (listed in STATUS.md's brand record); two were pulled into the wave (the tab bar's colour-only active state, and the settings screen's hand-rolled category chips).

## What the branch-level review found

Important (3, fixed in `4a7f110`):
- `apps/app/src/lib/fonts.ts` imported the five faces from the `@expo-google-fonts/*` barrels, which `require()` every weight and italic — Metro bundled all 32 static faces (2.9 MB in the web export) for the five the theme names (432 KB). Per-weight subpath imports; the export now carries exactly five `.ttf` files.
- The sidebar's active label rendered `primary` on `primaryTint` at 15 px — 4.46, the one pair the plan (deviation 6), `brand/brand.md` and STATUS all said no app text used. The active label is now `ink`/`text` in the 600 face (14.78), the icon keeps `primary`, and a test pins the weight.
- The bottom tab bar's active state was colour-only after the branch replaced the outline/filled Ionicons pair with one SVG variant per name, and `primary` vs `muted` differ in lightness by 1.05 (light) / 1.02 (dark) — below the 3:1 WCAG 1.4.1 accepts for a colour-alone cue. A custom `tabBarLabel` renders the focused label in the 600 face; the shell test's `js-tabs` mock invokes the callback and asserts the weight (what react-navigation itself passes as `focused` is not provable in jest and the test says so).

Minor (folded into the wave): no ISC notice accompanied the eight modified Lucide shapes — `brand/icons/LICENSE` now carries Lucide 1.44.0's licence text verbatim with the rewrite note, and `brand.md` §9 and `brand/README.md` point at it; the colour guard matched only `#RRGGBB`/`rgba(` — now `#RGB…#RRGGBBAA`, `rgb[a](`, `hsl[a](` (zero offenders); `brand/README.md` states the byte-for-byte PNG comparison assumes resvg's linux-x64 native build (CI's platform); the root layout's comment misdescribed the static export (server-side `useStaticFonts`, no null first frame); `brand.md`'s component default heights (24/20/24); `Mark`/`Wordmark` aspect ratios derived from the viewBox strings instead of duplicated literals; a one-line comment at `pages.ts`'s two constant interpolations.

Documented reading (no UI change, deviation 14 in STATUS): spec §6's "header lockup" is met by the wide layout's sidebar and the sign-in screen; a phone shows no brand chrome after sign-in because the tabs run `headerShown: false`, as before the branch. Whether the phone inbox gets a small header lockup is Robert's call (STATUS → *Open items for Robert*).

## Residuals parked after the re-review (with rulings)

- `responsive-shell.tsx`'s custom `tabBarLabel` no longer inherits react-navigation's default `numberOfLines={1}` — unreachable with three short fixed titles; add it if a title or an extreme accessibility font scale makes it reachable.
- The colour guard's file walk excludes `*.test.tsx`, so a `'#000'` mock argument in the shell test is unscanned — test files are deliberately out of scope (mock arguments never render).
- The sidebar's `menuitem` carries no `accessibilityState.selected`, so the active item's second cue is visual only for a screen-reader user — an unchanged line; deferred with the app's existing accessibility minors to Phase 7's pass.
- STATUS's fix-wave paragraph names the third commit as "this commit" because a commit cannot name its own SHA — it is `9b50dcd`, pinned here.
- `apps/worker/test/e2e-phase3.test.ts` case 10 is a pre-existing timing flake under the full parallel `pnpm test` (reproduced on the reviewed commit; passes standalone; no worker file changed on this branch) — belongs to `phase-3`'s follow-ups.

## Rulings

Five execution-time rulings (all in STATUS.md's brand record): the branch works in the main checkout rather than a worktree; the SVG regex helpers live in `brand/scripts/svg.ts` so the build imports rather than copies them; the fontWeight guard keeps `theme.ts` in scope and only its regex tightened; the app's `brand.test.tsx`/`icon.test.tsx` mock `react-native-svg` because RNTL 14 removed `UNSAFE_getByType` (the components and the shell test stay on the real library); Task 5's four test-tightening minors were folded into Task 6. The final review added three: one fix wave for everything above; spec §6's header lockup read as wide-layout-plus-sign-in with deviation 14 and an open item; the weak-ETag and unused-500-face notes left as notes (the 500 faces are registered per spec §4).

## What the review verified holds

One geometry chain end to end — `brand/scripts/compose.ts` → the committed `lockup-horizontal.svg` → `BRAND.paths.lockupHorizontal` → the app's `Lockup`, with `tokens.test.ts` recomputing the lockup from the sources and `build.test.ts` byte-comparing all 18 generated outputs (a stale file, a hand edit, or a changed icon set fails loudly); one token chain — `tokens.json` → `BRAND` (deep-equal) → the theme's palettes (every value a token of its theme) → chips, banners and buttons, and `BRAND` → the api's CSS variables — with no colour literal on any app surface outside `theme.ts`; the api's asset routes as a fixed six-URL allowlist read once at boot with ETag, a day of caching and 404s for siblings, resolved `import.meta.url`-relative so `tsx` from any cwd finds `public/`; the review pages with every dynamic interpolation still behind `esc()`, `friendlyPage()` still one constant, and no script or font network; the thirteen plan deviations sound and recorded in the plan, `brand.md` and STATUS (the adaptive icon's 35 % height verified inside Android's safe circle); the mocked-SVG tests asserting real composition against `BRAND` while the real `react-native-svg` is still mounted by the shell test and rendered by the smoke; pinned numbers (contrast ratios at two decimals, exact viewBox tuples, raster dimensions) that fail on drift; the trailer on every commit; the root-layout test correctly placed outside `src/app/` (a test file there would have become a route) with the 21-route count proving it.

Robert's manual check before the PR: open the web export signed in at a wide window and at phone width once — the tab icons, the sidebar lockup and the sign-in lockup have not been rendered in a browser by any gate (the smoke ends at the mailbox step, before the shell exists; the controller viewed `og.png` and `icon.png` only).
