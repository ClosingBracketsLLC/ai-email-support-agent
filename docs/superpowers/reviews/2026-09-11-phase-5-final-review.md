# Phase 5 — whole-branch final review record

Branch `phase-5`, base `ef81169` (`main`, after PR #5) → reviewed at `58dbe27` (18 commits: the plan `93bc1d0`, Task 1's two carries, Tasks 2–11 with their five one-round fix loops), fixed through `add8ad3`, then `ab17cd1` (the Phase 4 hand-off correction folded into STATUS). One reviewer on the most capable model over the whole branch in six passes (contracts + core + db; knowledge + agent; the worker jobs; the api; the app; the E2E + docs), with the plan, the spec's Phase 5 sections, CLAUDE.md and the execution ledger's ~55 deferred minors for triage. Gate at the reviewed commit: 2,355 tests plus 4 conditional skips, 24 web routes, the Playwright signup smoke green, `db:check` clean; after the wave, re-run on `ab17cd1`'s tree with `S3_*` exported: 2,364 tests plus 4 conditional skips (test-kit's), no drift, 24 routes.

## Verdict

Approved for the PR after the fix wave. Critical: none. Important: 6, all landed in one wave by ONE implementer (`03d2d2a` the seven code fixes across knowledge, worker, api, app and db; `add8ad3` the docs) and verdicted addressed A–H by the scoped re-review over `58dbe27..add8ad3` (no new Critical or Important breakage; two out-of-scope notes recorded below). The cross-task seams the per-task reviews could not see — the auto landing ↔ Hold ↔ re-approve ↔ `send.execute` ↔ the backstop ↔ the rollup — were traced by hand and hold.

## What the branch-level review found

Important (all fixed in the wave):
- **An auto-send inside its hold window survived its category's demotion or the owner switching it to Review.** `send.execute`'s `firstKillLever` acted only on `category_off`; a demotion thirty seconds into a fifteen-minute window let the reply go out unattended. Fixed with the lever `category_not_auto` (`draft.decision_source = 'auto'` and the policy no longer `auto`), landing through `landHeld` — send and draft held, one page, resumable; a Hold + re-approve rewrites `decision_source` to `app`, so a human-decided send is never affected. The app's hold copy gained the key.
- **"Should not have sent" was unreachable from the ticket screen.** The panel's only producer, `loadLiveDraftView`, never returned a `sent` draft. Widened: with nothing live and the ticket `waiting_on_customer`, the newest `sent` draft with `decision_source = 'auto'` is returned, bounded exactly like the `failed` fallback; `inbox.list`'s chip and the ticket screen's polling predicate are unaffected.
- **The autonomy helpers carried no `org_id` predicate but ran under RLS bypass in `stats.rollup`.** `readDemotionSignals` gained `orgId`; all three helpers filter it; a foreign-org test reads zero signals and demotes nothing.
- **Lost update on `operating_guidance`.** Both appenders (the reject-time append, `workspace.acceptSuggestion`) read the workspace row with a plain SELECT — `FOR UPDATE` on both, the workspace staying the last lock position.
- **Auto-sent activity never reached the daily digest email** (spec §Notifications: "auto-sent activity folds into the digest"). The digest now carries one line with the last 24 hours' auto-send count (`status = 'sent'`, `decision_source = 'auto'`, `auto_decided_at ≥ now − 24 h`), omitted at zero, computed inside the digest's existing read transaction; the count rides along with a digest and never creates one (ruled).
- **`drafts/service.ts` at ~1,120 lines** with the split seam (`src/drafts/learning.ts`) already named — ruled a Phase 6 cleanup, not a wave item.

Two items the close-out itself had surfaced were ruled into the same wave: `scrubForMemory` erased a one-line reply that OPENS with a sign-off word ("Thanks for getting in touch…" scrubbed to nothing, so `memory.capture` audited `empty_after_scrub`) — the sign-off cut now never removes the first content line and a candidate sign-off line qualifies only when at most three non-blank lines follow it; and the Activity screen never rendered `activity.summary.autoSent` — an "Auto-sent" tile.

Minor, folded into the wave: the E2E's send gate released in `try/finally` so one failed assertion cannot turn the remaining scenarios into timeouts; the runbook's "`.env.example` unchanged" wording ("no variable added; each gains a comment block"); STATUS's "one-item fix round" (Task 9's round addressed two), "four tabs" (three), the two close-out carries moved to closed, the `graduatedAt` vs `demotedAt` carry struck (the Autopilot screen already compares them); the `resolved_answers` schema header reworded to match CLAUDE.md's Memory rule (a reinforcing HUMAN approval re-sets `expires_at` to now + 365 d; reuse by a draft alone does not).

## Residuals parked after the review (with rulings)

- **Structure and duplication (Phase 6 cleanup task):** the `src/drafts/learning.ts` split (`maybeDemote`, `strikeUsedAnswers`, `flagAutoSent`, the guidance append); the evidence maths duplicated character for character between `ticket.draft` and `agent.sandbox` (a pure helper in `drafting/` would make sandbox parity structural); `meterValue` re-implemented in `ticket.draft`; `relativeTime` in four app files and `lookup()` in two.
- **Coverage gaps, none of which leave a spec promise unproven:** no test pins precedence among the three new `decide()` branches; `readDemotionSignals`'s `decisionWindowDays` boundary; the graduation sample's `decided_at desc` ordering; the `&& !auto` reinforce gate; `guidance.suggest`'s source-draft and `decision_source` skips; the E2E's scenario-1 scrub assertions cannot fail and its scenario-7 DMARC arm proves precedence over `below_threshold` rather than blocking of an otherwise-sendable draft; the `flags` demotion trigger has no end-to-end coverage (unit-covered in the api); the scrub's third bound (a candidate with more than three trailing lines is disqualified) has no dedicated row.
- **Behaviour the spec or the brief mandated, recorded:** `memoryScore` floors approvals; `auto_sent_flagged` counts on `flagged_at` alone (inert: `flagAutoSent` only ever flags a sent auto draft); `held` is gated on `auto_decided_at`'s window, not `auto_held_at`'s; the daily auto-send cap reads the delivered-count meter, so a burst inside one hold window can overshoot; `rejectCandidate` returns ok for a candidate retired mid-call (audited `alreadyRetired`); `SandboxOutputView.evidence` is required, so sandbox outputs stored before this deploy parse to `output: null`; `stats.rollup`'s counters and notification ids are bumped before `maybeNudge` runs in the same SAVEPOINT (log-only impact); `category_stats_daily`'s primary key does not lead with `org_id` (the secondary index does).
- **Hardening carried:** the reinforce UPDATE is not re-guarded on `status = 'active'` (a concurrent retire between the SELECT and the UPDATE bumps a retired row, which is never retrieved); `memory.skipped` audits `empty_after_scrub` even when the embedder returned no vector; `memory.capture`'s embed is neither metered under `KNOWLEDGE_METERS.embedTokens` nor capped; `sweeps.daily` arm (f) matches `knowledge_chunks` by id alone under RLS bypass and is an unbounded nightly scan (Phase 7 class, like `ROLLUP_ORGS_PER_RUN` and the per-org draft load); `activity.summary` excludes auto drafts from the approved buckets only because `edit_distance_ratio` is NULL on them; `guidance.suggest` reads the platform killswitch but not the workspace's; `countHumanDecisions` carries no `org_id` predicate (called only under `withOrg`); `ensureCustomerHashSalt` mints a salt even when the customer email is null; `graduateCategory` leaves `demoted_at`/`demoted_reason` standing on re-graduation (harmless: the demotion sentence is gated on `mode = 'review'`); answers skipped as `empty_after_scrub` before the scrub fix are not backfilled (`memory_captured_at` is stamped; a re-capture job is Phase 7's "Remember this reply" territory).
- **App polish:** the mode/preset radios give no in-flight feedback; the suggestion CTA is not disabled under the cold-start lock (inert: a suggestion needs twenty human decisions); one screen-level error banner for per-category failures and `errorCode` not cleared on an agent switch; "Deleted 1 answers"; the delete-by-customer armed state survives a tab switch; `formatCountdown` has no NaN guard (unreachable); `autopilot.tsx` writes `AUTONOMY_THRESHOLD_PRESETS.balanced` where `DEFAULT_AUTO_SEND_THRESHOLD` is the named constant; no tests for the `agent_inactive`/generic error copy, a failed `dismissSuggestion`, or the Active/Retired empty strings.
- **Carried from Phase 4 unchanged:** the stuck-source sweep, the org-cap arm of the backstop busy loop, the in-memory api rate limiters, `platform.access` audit retention, the better-auth ↔ drizzle peer bump, `keys.test.ts` order-dependence, the app accessibility minors.

## Rulings

Nineteen execution-time rulings, all in STATUS.md's Phase 5 record. The ones the final review added or confirmed: the six Important findings' dispositions above; the two close-out items ruled into the wave; the digest count folding into a digest without creating one; the auto-send fixture seeding an `auto` policy (the old fixture described a state the product cannot produce); no backfill of pre-fix skipped answers. Earlier, load-bearing: the global lock order gained a fourth position, `outbound_sends → drafts → tickets → resolved_answers / workspaces`, when Task 9's `rejectDraft` was found to lock answers before the ticket while the worker's `applyDraftOutcome` locks the ticket then flags a conflicting answer in one transaction (a real 40P01 cycle) — `rejectDraft` reordered and wrapped in `withDeadlockRetry`, CLAUDE.md's rule rewritten; Task 8's plan recipe was defective against the spec's per-day table (an instant cutoff plus a full-replace upsert would have shaved a day of history every night) — the cutoff is the UTC midnight of now − 30 d and `drafted` is guarded on `created_at ≥ cutoff`; Task 6's backstop orphan arm (c) widened to `auto_sending` so a levered auto-send whose held draft expires unresumed pages `orphaned` instead of dropping the customer's email; Task 7's `guidance.suggest` made to honour the global killswitch; Task 4's scrub deviations (the one-half sign-off window; phones need a separator) accepted and then the window's defect caught by the E2E and fixed in the wave.

## What the review verified holds

Tenancy: every new read and write inside `withOrg` or a `withPlatform` SAVEPOINT lent one org's identity; every SQL predicate carries `org_id` (the autonomy helpers now included); the answers leg filters `org_id`, `status = 'active'` and `embedding_model` in SQL, re-reads under `withOrg` with `status = 'active'` re-applied, and `assertSameOrg` throws on any foreign row; no global vector index. Guarded writes on every status flip, the four-position lock order in the worker and the api, no network I/O inside a transaction (`memory.capture` embeds between its two transactions, `guidance.suggest` calls the model between its cap and its insert, the retriever's answers leg lives inside the chunk legs' two transactions), every entry into `needs_owner` through `escalateTicket`, the three guardrail gates untouched, every model-calling job reading the platform killswitch. `decide()`'s order is the spec's with the three blockers after `guardrail_warning`; `drafts.confidence` still the model's number and `confidence_breakdown.evidence` the number the auto gate compares; nothing but the auto landing writes `decision_source = 'auto'`. PII: no customer body in any audit row or log (lengths and counts only), the customer-hash salt never leaves the server, only scrubbed text in `resolved_answers`, delete-by-customer by salted hash with a count audited. The four-places queue rule for `memory.capture` and `guidance.suggest` with `policy: 'short'` on both pre-creates; migrations 0016/0017 sound (FORCE RLS and the two policies on the three new tables, the `notifications_kind_check` DROP/ADD, the three partial indexes), `db:check` in sync. The app never value-imports a server package, sets no `fontWeight`, writes no literal colour; the two push categories split (`draft_review` Review only; `auto_send` Review + Hold) and the worker stamps exactly those. The E2E proves the spec's Verify list through the real jobs and the real api services with no sleeps, an isolated pg-boss schema and cleanup scoped to its own org ids; the two `act()` warnings in `inbox.test.tsx` pre-date the branch (its only change is one copy string).

Robert's manual checks before real tenants (in `docs/runbooks/2026-09-phase-5-external-setup.md`): the live walk on both providers — three approvals of one question, the fourth auto-sending with the inbox countdown, a Hold from the phone's push (`notifications.push_auto_sends` on for the walk), a real auto-send landing threaded with the agent's signature, "Should not have sent" retiring the answer and the second flag demoting; the privacy-policy line for stored, scrubbed answers (365 days, salted customer hash, delete-by-customer as the interim erasure path).

## Adjudication after the re-review

The scoped re-review verdicted every wave item ADDRESSED with no new breakage and left two out-of-scope notes: the scrub's third bound has no dedicated test row (parked above), and STATUS struck the `graduatedAt` vs `demotedAt` carry without a code change (harmless — the demotion sentence is gated on `mode = 'review'`; `graduateCategory` still leaves the stamps standing; parked above). After the wave the controller folded Robert's Phase 4 hand-off correction (`d5d58e0`: the phase count and where each phase stands) into STATUS's intro and headings as `ab17cd1` and deleted the local `docs/phase-4-handoff` branch as he had asked; the full gate was re-run green on that tree before the finishing menu.
